/**
 * Tutorial (boas-vindas em `HomePage` + coachmarks em `QuemSouEuPage`) desligado a
 * pedido do usuário enquanto ele ajusta o conteúdo — nada do tutorial foi removido,
 * só o gatilho automático. Volte para `true` para reativar os dois.
 */
export const TUTORIAL_ATIVO = false;
